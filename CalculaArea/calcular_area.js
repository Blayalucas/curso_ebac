/* Biblioteca */
    const prompt = require ('prompt-sync')()

/* Função e constante */

function calculaArea (base,altura) {
    const base = prompt()
    const altura = prompt()
    const total = base * altura
    return total
}
/* Perguntas desejadas */
console.log (" Digite o valor de sua Base   ") {
    const base = number (prompt("base"))

console.log ("Digite o valor de sua altura  ")   
    const altura = number (prompt("altura"))

/* Resposta */
console.log (" Sua área será: ")
    const total = calculaArea (base, altura)

    return total
}

calculaArea ()

